<nav class="navbar navbar-expand-lg navbar-dark bg-primary navMain">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">FunEvents</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="nav-item">
            <NavLink class="nav-link" style="text-align: left; color: white; font-weight: bold" href="/" Match="NavLinkMatch.All">
                <span></span>Home
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" style="text-align: left; color: white; font-weight: bold" href="/about" Match="NavLinkMatch.All">
                <span></span>Informationen
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" style="text-align: left; color: white; font-weight: bold" href="contact" Match="NavLinkMatch.All">
                <span></span>Kontakt
            </NavLink>
        </div>
        @if (Model.isLoggedin)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" style="text-align: end; color: white; font-weight: bold" href="" Match="NavLinkMatch.All">
                    <span aria-hidden="true"></span> Meine Buchungen
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink id="navbaritem" class="nav-link" style="text-align: end; color: white; font-weight: bold" href="counter">
                    <span aria-hidden="true"></span> Events buchen
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" id="navbaritem" style="text-align:end; color:white; font-weight:bold" href="fetchdata">
                    <span aria-hidden="true"></span> Benutzerkonto
                </NavLink>
            </div>
        }
        else
        {
            <div class="nav-item px-3">
                <NavLink id="navbaritem" class="nav-link" style="text-align: end; color: white; font-weight: bold" href="register">
                    <span aria-hidden="true"></span> Registrierung
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" id="navbaritem" style="text-align:end; color:white; font-weight:bold" href="login">
                    <span aria-hidden="true"></span> Login
                </NavLink>
            </div>
        }
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    public class Model
    {
        public static bool isLoggedin = false;
    }

}